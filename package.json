{
  "name": "electron-vue-parcel-boilerplate",
  "version": "0.3.0",
  "description": "A boilerplate for Electron + Vue + Parcel.",
  "author": "shamofu",
  "license": "MIT",
  "private": true,
  "main": "app/main/.parcel/main.js",
  "renderer": "app/renderer/.parcel/index.html",
  "targets": {
    "main": {
      "context": "electron-main"
    },
    "renderer": {
      "context": "browser"
    }
  },
  "scripts": {
    "start": "run-s main:build electron:watch",
    "test": "run-s parcel:build \"test:jest {@}\" --",
    "release": "run-s parcel:build \"electron:build {@}\" --",
    "electron:watch": "run-p -r parcel:watch electron:start",
    "electron:start": "nodemon --watch app/main/.parcel --exec \"electron .\"",
    "electron:build": "electron-builder --x64 --publish=never",
    "parcel:build": "run-s main:build renderer:build",
    "parcel:watch": "run-p -r main:watch renderer:watch",
    "main:build": "parcel build app/main/main.ts --target main",
    "main:watch": "parcel watch app/main/main.ts --target main",
    "renderer:build": "parcel build app/renderer/index.html --public-url ./ --target renderer",
    "renderer:watch": "parcel serve app/renderer/index.html --port 1124 --target renderer",
    "test:jest": "jest"
  },
  "build": {
    "productName": "ElectronVueParcel",
    "appId": "org.shamofu.ElectronVueParcel",
    "files": [
      "app/**",
      "!app/main/main.ts",
      "!app/main/tests/**",
      "!app/renderer/**",
      "app/renderer/.parcel/**",
      "!app/**/*.map"
    ],
    "mac": {
      "target": [
        "zip"
      ],
      "artifactName": "${productName}-mac.${ext}"
    },
    "win": {
      "target": [
        "zip"
      ],
      "artifactName": "${productName}-win.${ext}"
    },
    "linux": {
      "target": [
        "zip"
      ],
      "category": "Development",
      "artifactName": "${productName}-linux.${ext}"
    },
    "directories": {
      "output": "release"
    }
  },
  "jest": {
    "verbose": true,
    "collectCoverageFrom": [
      "app/renderer/**",
      "!app/renderer/index.ts",
      "!app/renderer/tests/**",
      "!app/renderer/.parcel/**"
    ],
    "transform": {
      "^.+\\.tsx?$": "babel-jest",
      "^.+\\.vue$": "vue-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.tsx?$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "vue",
      "json",
      "node"
    ]
  },
  "dependencies": {
    "app-root-path": "^3.0.0",
    "electron-is-dev": "^2.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.13.15",
    "@babel/preset-env": "^7.13.15",
    "@types/jest": "^26.0.22",
    "@vue/test-utils": "^1.1.4",
    "babel-jest": "^26.6.3",
    "electron": "^12.0.4",
    "electron-builder": "^22.10.5",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0-beta.2",
    "spectron": "^14.0.0",
    "typescript": "^4.2.4",
    "vue": "^2.6.12",
    "vue-jest": "^3.0.7",
    "vue-router": "^3.5.1",
    "vue-template-compiler": "^2.6.12"
  }
}
